<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedSwift"
        tools:targetApi="31">
        <activity
            android:name=".screens.NearbyDoctorsScreen"
            android:exported="false"
            android:label="@string/title_activity_nearby_doctors_screen"
            android:theme="@style/Theme.MedSwift" />


        <activity
            android:name=".screens.user_auth.RegisterScreen"
            android:exported="true" />

        <activity
            android:name=".screens.nearby_doctors.NearbyDoctorsScreen"
            android:exported="true" />

        <activity
            android:name=".screens.HomeScreen"
            android:exported="true" />

        <activity
            android:name=".screens.symptoms.SymptomsActivity"
            android:exported="true"
            android:label="@string/title_activity_login_screen"
            android:theme="@style/Theme.MedSwift" />

        <activity
            android:name=".screens.user_auth.LoginActivity"
            android:exported="true"
            android:label="@string/title_activity_login_screen"
            android:theme="@style/Theme.MedSwift">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MedSwift" />

    </application>

</manifest>